version: '3.3'

volumes:
  mongodb-storage:

services:
  ing.des.test:
    ports:
      - '8080:80'
    container_name: ing.des.test
    volumes:
      - $PWD/app
    image: ing.des.test
    environment:
      - 'MONGO_USER=user'
      - 'MONGO_PASS=ergSm@e&EB@2PmSYG!'
      - 'MONGO_HOST=78.47.131.57'
      - 'MONGO_PORT=27017'
      - 'MONGO_DB_NAME=ing-des'
  mongodb:
    image: mongo:latest
    container_name: "mongodb"
    environment:
      MONGO_DATA_DIR: '/data/db'
      MONGO_LOG_DIR: '/dev/null'
      MONGODB_USER: "user"
      MONGODB_PASS: "ergSm@e&EB@2PmSYG!"
    volumes:
      - mongodb-storage:/data/db
    ports:
      - 27017:27017
